<!--en rett-->
<div class="flex lg:py-24  pt-20 justify-center">

  <div class="flex flex-col bg-white rounded-2xl lg:w-1/3">
    <div>
      <% if @menu_item.image.attached? %>
        <%= image_tag @menu_item.image, class:"shadow object-contain h-64 rounded-t-2xl w-full align-middle border-none"%>
      <% end %>
    </div>
    <div class="m-10">
      <div class="flex flex-row justify-between">
        <div class="font-semibold text-2xl">
          <%= @menu_item.name %>
        </div>

        <div class="font-semibold text-xl">
          <%= @menu_item.price.to_i %>,-
        </div>
      </div>

      <div class="text-gray-700 pt-10">
        <%= @menu_item.description %>
      </div>

      <div class="text-gray-700 pt-10">
        Allergener:
        <br>
        <%= @menu_item.get_allergen_names_long.join(', ') %>
      </div>

      <%= react_component("add_to_cart", { menuItem: @menu_item }) %>
    </div>
  </div>

</div>
