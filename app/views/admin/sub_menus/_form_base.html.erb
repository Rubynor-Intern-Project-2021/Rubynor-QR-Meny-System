<% if sub_menu.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(sub_menu.errors.count, "error") %> prohibited this sub menu from being saved:</h2>

    <ul>
      <% sub_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<div>
  <div class="flex flex-col rounded-2xl lg:w-1/3">


    <div class="mx-10">

      <div class="field text-gray-700 pt-5">
        <% form.label :menu_id, class: 'text-gray-700'  %>
        <%= form.text_field :menu_id, class: 'w-80 border-gray-800 rounded-lg'  %>
      </div>
      <div class="field text-gray-700 pt-5">
        <%= form.label "Navn", class: 'text-gray-700' %><br>
        <%= form.text_field :name, class: 'w-80 border-gray-800 rounded-lg' %>
      </div>
      <div class="field text-gray-700 pt-5">
        <%= form.label "Rekkefølge", class: 'text-gray-700' %><br>
        <%= form.text_field :number, class: 'w-80 border-gray-800 rounded-lg' %>
      </div>

      <div class="actions text-gray-700 pt-5">
        <%= form.submit "Lagre", class: 'btn-indigo' %>
      </div>
    </div>
  </div>
</div>
