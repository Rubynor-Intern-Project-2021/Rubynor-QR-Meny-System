<% if menu.errors.any? %>
  <div class="w-80 p-5 border-2 border-red-600 text-red-600">
    <h2 class="text-red-600"> <%= pluralize(menu.errors.count, "error") %> prohibited this menu from being saved:</h2>
    <ul>
      <% menu.errors.full_messages.each do |message| %>
        <li class="text-red-600"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
  
<div class="flex flex-col rounded-2xl lg:w-1/3">
  <div class="w-96">
    <% if menu.image.attached? %>
      <%= image_tag menu.image,  class: "shadow object-fill w-full border-none"%>
    <% end %>
  </div>

  <div class="space-y-3">
    <div class="field">
      <%= form.label "Bilde" %><br>
      <%= form.file_field :image %>
    </div>
    <div class="field">
      <% #form.label :restaurant_id, class: 'text-gray-700' %>
      <div>
        <%= form.number_field :restaurant_id, class: 'w-80 border-gray-800 rounded-lg', type: :hidden%>
      </div>
    </div>

    <div class="field">
      <%= form.label "Navn", class: 'text-gray-700'  %>
      <div>
        <%= form.text_field :name, class: 'w-80 border-gray-800 rounded-lg' %>
      </div>
    </div>

    <div class="field">
      <%= form.label "Rekkefølge", class: 'text-gray-700' %>
      <div>
        <%= form.text_field :number, class: 'w-80 border-gray-800 rounded-lg' %>
      </div>
    </div>

    <div class="field">
      <%= form.label "Beskrivelse", class: 'text-gray-700' %>
      <div>
        <%= form.text_area :description, class: 'w-80 border-gray-800 rounded-lg' %>
      </div>
    </div>

    <div class="">
      <%= form.submit "Lagre", class: 'btn-indigo' %>
    </div>
  </div>
</div>
