<% if allergen.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(allergen.errors.count, "error") %> prohibited this allergen from being saved:</h2>

    <ul>
      <% allergen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<div  >
  <div class="flex flex-col rounded-2xl lg:w-1/3">


    <div class="mx-10">
      <div class="field text-gray-700 pt-5">
        <%= form.label :name, class: 'text-gray-700' %><br>
        <%= form.text_field :name, class: 'w-80 border-gray-800 rounded-lg' %>
      </div>

      <div class="field text-gray-700 pt-5">
        <%= form.label :short_name, class: 'text-gray-700' %><br>
        <%= form.text_field :short_name, class: 'w-80 border-gray-800 rounded-lg' %>
      </div>

      <div class="actions text-gray-700 pt-5">
        <%= form.submit class: 'btn-indigo' %>
      </div>
    </div>
  </div>
</div>
