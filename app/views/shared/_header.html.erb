<nav class="flex items-center justify-between flex-wrap bg-white lg:bg-gray-800 p-2 fixed w-full z-10 pin-t border-b border-gray-200 z-50">

  <div class="flex items-center flex-no-shrink text-gray-900 lg:text-gray-100 mr-6">
    <%= link_to @restaurant.name, restaurant_path(:id => @restaurant.id), class: "text-md lg:text-2xl pl-2 font-semibold uppercase text-gray-900 lg:text-white hover:text-gray-600 lg:hover:text-gray-200 no-underline hover:text-white hover:no-underline" %>
  </div>
  <div class="mr-3">
    <%= link_to order_path(:id => @restaurant.id) do %>
      <%= react_component("cart_icon/cart_icon") %>
    <% end %>
  </div>

  <div class="w-full flex-grow lg:flex lg:items-center lg:w-auto hidden lg:block pt-6 lg:pt-0 ">
    <ul class="list-reset lg:flex justify-end flex-1 items-center">
      <li class="mr-3">
        <%= link_to 'Meny', restaurant_path(:id => @restaurant.id), class: 'nav-btn' %>
      </li>
      <% if session[:restaurant_id] != nil %>
        <li class="mr-3 hidden lg:block text-gray-200">|</li>
        <li class="mr-3 block lg:hidden">
          <hr style="height:2px;border-width:0;color:gray;background-color:#808080"/>
        </li>
        <li class="mr-3">
          <%= link_to 'Admin', admin_restaurant_path(:id => @restaurant.id), class: "nav-btn" %>
        </li>
        <li class="mr-3">
          <%= link_to 'Ordre', admin_order_path(:id => @restaurant.id), class: "nav-btn" %>
        </li>
        <li class="mr-3">
          <%= link_to 'Logg ut', logout_path, method: :delete, class: "nav-btn" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

